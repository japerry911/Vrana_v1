import React from 'react';
import { useStyles } from './AdminStyles';

const Admin = () => {
    const classes = useStyles();

    return (
        <div>
            <h1>Admin Page</h1>
        </div>
    );
};

export default Admin;